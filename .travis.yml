language: node_js
node_js:
  - node
script:
  - "npm test"
    #after_success:
    #  - "npm run coveralls"
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - libcairo2-dev
      - libjpeg8-dev
      - libpango1.0-dev
      - libgif-dev
      - g++-7
